# ตั้งค่าตัวตรวจโค้ด (analyzer) — สั่งตรวจด้วย `flutter analyze`
# ใช้ชุดกฎมาตรฐานที่ Flutter แนะนำ
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # เปิด/ปิดกฎ lint เพิ่มเติมได้ที่นี่ เช่น:
    # prefer_single_quotes: true
